~~ comment
main : -> int {
 
  regexp zero;
  regexp one;
  regexp x;
  regexp y;
  regexp z;

  regexp t;
  regexp w;
  regexp s;

  zero  = {.};
  one = {{.}};
  printr(zero);
  printr(one);

  x = lit 'c';
  y = lit 'a';
  z = x ^ y;

  t = x | z;
  w = y & t;
  s = w **;

  printr(s);

  return (0);
}
