~~ comment
main : -> int {
 
  regexp x;
  regexp y;
  regexp z;
  regexp t;
  regexp w;
  regexp s;

<~
  string s;
  regexp m;
~>

  x = lit 'c';
  y = lit 'a';
  z = x ^ y;
  t = x | z;
  w = y & t;
  s = w **;

<~
  printr(x);
  printr(y);
~>

  printr(s);

  x = lit 'b';

~~ testing if w gets modified as well
~~ printr(s);

  
  return (0);
}
